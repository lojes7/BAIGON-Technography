# 百工谱 — crawler_service 依赖
# Python + FastAPI + gRPC

fastapi==0.115.0
uvicorn[standard]==0.30.0
# 1.65.0 已被 PyPI 撤回（grpc/grpc#37178），使用同一修复系列的稳定版本。
grpcio==1.65.1
grpcio-tools==1.65.1
protobuf==5.27.0
python-consul==1.1.0
kafka-python==3.0.9  # 2.0.2 在 Python 3.12 上 import 崩溃（kafka.vendor.six.moves 缺失），升级到维护版
sqlalchemy==2.0.32  # ORM，同步驱动 psycopg2
psycopg2-binary==2.9.9
pgvector==0.3.2  # PostgreSQL vector(1024) 的 SQLAlchemy/psycopg2 映射
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.3.0
DrissionPage==4.1.1.4  # 智联招聘爬虫（Chromium 自动化 + 网络监听）
python-dotenv==1.0.1
pydantic==2.8.0
pydantic-settings==2.4.0
